from pathlib import Path

REPO_ROOT = Path(__file__).resolve().parents[3]
CRAWLABLE_ROOT = REPO_ROOT / "crawlable"
REPRESENTATION_ROOT = REPO_ROOT / "representation"
PLATFORM_ROOT = REPO_ROOT / "platform"
